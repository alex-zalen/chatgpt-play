# chatgpt-play

This repository contains small experiments. The `app.py` module runs a simple Flask webapp that displays the current date and time in Rotterdam with a futuristic style.

## Running the app

```bash
pip install flask
python app.py
```

Open `http://localhost:5000` in your browser.
